# Required: Claude API access
ANTHROPIC_API_KEY=
# Required: MCP server OAuth tokens (obtain via MCP Inspector OAuth flow)
LINEAR_MCP_TOKEN=
# Required: PAT token for MCP server
GITHUB_MCP_PAT=

# Optional: Override defaults
# MODEL=claude-sonnet-4-6
# MAX_TOKENS=16384
